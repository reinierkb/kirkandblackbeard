<!doctype html>
<html lang="{{ language or metadata.language }}">
    {% include '../head.njk' %}
    <body>
        {% include '../header.njk' %}
        <main class="home">
            {{ content | safe }}
        </main>
        <!-- Current page: {{ page.url | url }} -->
        {% include '../footer.njk' %}
    </body>
</html>
